import * as React from "react";
import * as ReactDOM from "react-dom";

const element = (
    <div>Green is the prime color o the world</div>
);

ReactDOM.render(element, document.getElementById('root'));
  